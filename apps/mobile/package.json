{
  "name": "mobile",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "sync": "node scripts/copy-web-build.js && npx cap sync android",
    "open:android": "npx cap open android",
    "build:apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
