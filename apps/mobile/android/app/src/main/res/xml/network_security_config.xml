<?xml version="1.0" encoding="utf-8"?>
<!--
  DEVELOPMENT ONLY: permits plain-HTTP traffic to the specific LAN addresses used to reach
  the Windows dev machine's backend during local pilot testing. Production should serve the
  API over HTTPS and this file (and the networkSecurityConfig reference in
  AndroidManifest.xml) should be removed or scoped down at that point.

  Update the <domain> entries below if the dev machine's LAN IP changes (see `ipconfig` and
  apps/web/.env.mobile).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.207.155.8</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
